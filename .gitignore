# ===== 대용량 데이터 (GitHub 100MB 제한 초과 / 앱 실행에 불필요) =====
# 앱은 insight_data.pkl + model_3class/ 만으로 동작합니다.
# 아래 CSV들은 인덱스를 '다시 만들' 때만(build_book_index.py) 필요합니다.
*.csv
reviews_translated_cache.csv
train_tokenized_cache.csv

# 학습 체크포인트 (앱은 sa_3class_model.keras 만 사용 → best 는 제외)
model_3class/best_3class.keras

# 파이썬 / 주피터 캐시
__pycache__/
*.pyc
.ipynb_checkpoints/

# 임시 산출물
_tmp_*/
_slides_png/

# Streamlit 비밀키
.streamlit/secrets.toml
